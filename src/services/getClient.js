import { apiConfig } from "./api-config.js";
import { getId } from "../modules/submit.js";

export async function getClient(id) {
  try {
    
  } catch (e) {
    console.log(e);
    alert("Não foi possível consultar o cliente")
  }
}